node_modules/
.next/
next-env.d.ts
*.tsbuildinfo
__pycache__/
.DS_Store
.atelier-choix.json
.atelier-licence.json
# Porte le jeton de rafraîchissement du compte connecté.
.atelier-acces.json
.claude/launch.json

# Les secrets du rôle service. `.env.exemple` reste suivi, lui : il ne porte
# que des emplacements vides et les liens pour les remplir.
.env
.env*.local


# clerk configuration (can include secrets)
/.clerk/

# Reliquat du VPS, qui n'est plus déployé d'ici. S'il traîne encore sur un
# poste, il ne part jamais.
.env.production

# Le paquet npm assemblé — reconstruit à chaque publication.
paquet/
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# Le dist de la charte est reconstruit par `npm run build --prefix charte`.
charte/dist/
